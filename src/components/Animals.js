
function Animals({category}) {
    return (
        <div>
            {category.Animal}
            {/* <img src={category.animal.img} alt={category.animal.name} /> */}
        </div>
    )
}

export default Animals